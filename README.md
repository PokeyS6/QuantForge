# QuantForge

QuantForge is a Python project scaffold for future quantitative research tooling.

This initial foundation intentionally includes only packaging, importability, and test setup.

## Development

Install the package with development dependencies:

```bash
python -m pip install -e ".[dev]"
```

Run tests:

```bash
python -m pytest
```
